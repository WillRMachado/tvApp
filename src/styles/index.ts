import {structure, measures} from './structure';
import mainTheme from './themes/main';

const globalStyles = {
  structure,
};

export {mainTheme, measures};
export type {ThemeColorsTypes, ThemesType} from './themes/themesType';
export default globalStyles;
